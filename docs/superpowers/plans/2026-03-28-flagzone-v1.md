# FlagZone V1 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build and deploy flagzone.de — a German-language flag football content site with 6 SEO-optimized pages, affiliate-ready equipment page, and interactive team finder.

**Architecture:** Next.js 14 App Router with static generation. Editorial content as MDX files rendered via next-mdx-remote. Structured data (teams, events) as JSON files. Tailwind CSS for styling with a custom design system based on the approved brand (navy #002B5C + gold #FFC72C). Cookie-free analytics via Vercel Analytics.

**Tech Stack:** Next.js 14, TypeScript, Tailwind CSS, next-mdx-remote, Vercel Analytics, Vercel deployment.

**Design spec:** `docs/superpowers/specs/2026-03-28-flagzone-v1-design.md`
**Brand preview:** `brand-preview.html` (approved visual direction)

---

## File Structure

```
flagfootball/
├── public/
│   ├── images/                    # Placeholder images, favicon
│   │   ├── favicon.svg
│   │   ├── og-default.jpg         # Default OG image (placeholder)
│   │   └── placeholder-hero.jpg   # Hero background placeholder
│   └── robots.txt                 # Static robots.txt fallback
├── src/
│   ├── app/
│   │   ├── layout.tsx             # Root layout: nav, footer, fonts, metadata
│   │   ├── page.tsx               # Startseite
│   │   ├── was-ist-flag-football/
│   │   │   └── page.tsx           # Rules/explainer page
│   │   ├── olympia-2028/
│   │   │   └── page.tsx           # Olympics pillar page
│   │   ├── wm-2026-duesseldorf/
│   │   │   └── page.tsx           # World Championship page
│   │   ├── ausruestung/
│   │   │   └── page.tsx           # Equipment + affiliate page
│   │   ├── team-finden/
│   │   │   └── page.tsx           # Team finder (client component for interactivity)
│   │   ├── impressum/
│   │   │   └── page.tsx           # Legal page
│   │   ├── datenschutz/
│   │   │   └── page.tsx           # Privacy policy
│   │   └── sitemap.ts             # Dynamic sitemap generation
│   ├── components/
│   │   ├── Nav.tsx                # Sticky nav with logo + links
│   │   ├── Footer.tsx             # Site footer
│   │   ├── JsonLd.tsx             # JSON-LD structured data helper
│   │   └── TeamFinder.tsx         # Interactive team search/filter (client component)
│   └── data/
│       ├── teams.json             # Team directory data
│       └── events.json            # Upcoming events data
├── tailwind.config.ts             # Custom theme (colors, fonts)
├── next.config.mjs                # Next.js config (MDX if needed later)
├── tsconfig.json                  # TypeScript config
├── package.json
└── .gitignore
```

---

## Task 1: Project Scaffold

**Files:**
- Create: `package.json`, `tsconfig.json`, `tailwind.config.ts`, `next.config.mjs`, `.gitignore`, `postcss.config.mjs`
- Create: `src/app/layout.tsx`, `src/app/page.tsx`
- Create: `public/favicon.svg`

- [ ] **Step 1: Initialize Next.js project**

Run from the project root `/Users/konrad/Documents/06_Stuff/flagfootball`:

```bash
npx create-next-app@14 . --typescript --tailwind --eslint --app --src-dir --no-import-alias --use-npm
```

When prompted, accept defaults. This will scaffold into the current directory. It may ask to overwrite README.md — that's fine.

- [ ] **Step 2: Verify the scaffold works**

```bash
npm run dev
```

Expected: Dev server starts on http://localhost:3000, shows the default Next.js page. Stop the server with Ctrl+C.

- [ ] **Step 3: Configure Tailwind theme with FlagZone brand**

Replace the contents of `tailwind.config.ts`:

```typescript
import type { Config } from "tailwindcss";

const config: Config = {
  content: [
    "./src/**/*.{js,ts,jsx,tsx,mdx}",
  ],
  theme: {
    extend: {
      colors: {
        navy: {
          DEFAULT: "#002B5C",
          light: "#0a3a6b",
        },
        gold: {
          DEFAULT: "#FFC72C",
          hover: "#e6b000",
        },
        bg: {
          DEFAULT: "#fafaf9",
          warm: "#f5f5f0",
        },
        muted: "#52525b",
      },
      fontFamily: {
        sans: ["Inter", "system-ui", "-apple-system", "sans-serif"],
      },
    },
  },
  plugins: [],
};

export default config;
```

- [ ] **Step 4: Create favicon**

Create `public/favicon.svg`:

```svg
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 32 32">
  <rect width="32" height="32" rx="6" fill="#002B5C"/>
  <text x="4" y="23" font-family="Inter,system-ui,sans-serif" font-weight="900" font-size="14" fill="#ffffff">F</text>
  <text x="15" y="23" font-family="Inter,system-ui,sans-serif" font-weight="900" font-size="14" fill="#FFC72C">Z</text>
</svg>
```

- [ ] **Step 5: Clean up default scaffold files**

Delete the default Next.js page content and global styles. Replace `src/app/globals.css`:

```css
@tailwind base;
@tailwind components;
@tailwind utilities;

@import url('https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800;900&display=swap');

@layer base {
  html {
    scroll-behavior: smooth;
    -webkit-font-smoothing: antialiased;
  }

  body {
    @apply bg-bg text-[#18181b] font-sans;
  }

  ::selection {
    @apply bg-gold/30;
  }
}
```

Replace `src/app/page.tsx` with a minimal placeholder:

```tsx
export default function Home() {
  return (
    <main>
      <h1 className="text-4xl font-black text-navy p-8">FlagZone</h1>
      <p className="text-muted px-8">Dein Einstieg in Flag Football</p>
    </main>
  );
}
```

- [ ] **Step 6: Verify brand colors render**

```bash
npm run dev
```

Expected: Page shows "FlagZone" in navy with muted tagline on off-white background. Stop server.

- [ ] **Step 7: Commit**

```bash
git add -A
git commit -m "feat: scaffold Next.js project with FlagZone brand theme"
```

---

## Task 2: Shared Layout — Nav & Footer

**Files:**
- Create: `src/components/Nav.tsx`
- Create: `src/components/Footer.tsx`
- Modify: `src/app/layout.tsx`

- [ ] **Step 1: Create Nav component**

Create `src/components/Nav.tsx`:

```tsx
"use client";

import Link from "next/link";
import { useState } from "react";

const NAV_ITEMS = [
  { href: "/was-ist-flag-football", label: "Was ist Flag Football?" },
  { href: "/olympia-2028", label: "Olympia 2028" },
  { href: "/wm-2026-duesseldorf", label: "WM 2026" },
  { href: "/ausruestung", label: "Equipment" },
  { href: "/team-finden", label: "Team finden" },
];

export function Nav() {
  const [menuOpen, setMenuOpen] = useState(false);

  return (
    <nav className="bg-navy sticky top-0 z-50">
      <div className="max-w-6xl mx-auto px-4 sm:px-6 flex items-center justify-between h-14">
        <Link href="/" className="text-xl font-black tracking-tight">
          <span className="text-white">Flag</span>
          <span className="text-gold">Zone</span>
        </Link>

        {/* Desktop nav */}
        <ul className="hidden md:flex items-center gap-6">
          {NAV_ITEMS.map((item) => (
            <li key={item.href}>
              <Link
                href={item.href}
                className="text-white/75 text-sm font-medium hover:text-gold transition-colors"
              >
                {item.label}
              </Link>
            </li>
          ))}
        </ul>

        {/* Mobile hamburger */}
        <button
          className="md:hidden text-white/80 hover:text-gold"
          onClick={() => setMenuOpen(!menuOpen)}
          aria-label="Menu"
        >
          <svg className="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            {menuOpen ? (
              <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M6 18L18 6M6 6l12 12" />
            ) : (
              <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M4 6h16M4 12h16M4 18h16" />
            )}
          </svg>
        </button>
      </div>

      {/* Mobile menu */}
      {menuOpen && (
        <div className="md:hidden border-t border-white/10 px-4 pb-4">
          {NAV_ITEMS.map((item) => (
            <Link
              key={item.href}
              href={item.href}
              className="block py-2 text-white/75 text-sm font-medium hover:text-gold transition-colors"
              onClick={() => setMenuOpen(false)}
            >
              {item.label}
            </Link>
          ))}
        </div>
      )}
    </nav>
  );
}
```

- [ ] **Step 2: Create Footer component**

Create `src/components/Footer.tsx`:

```tsx
import Link from "next/link";

const FOOTER_LINKS = [
  { href: "/was-ist-flag-football", label: "Regeln" },
  { href: "/olympia-2028", label: "Olympia 2028" },
  { href: "/wm-2026-duesseldorf", label: "WM 2026" },
  { href: "/ausruestung", label: "Equipment" },
  { href: "/team-finden", label: "Team finden" },
];

const LEGAL_LINKS = [
  { href: "/impressum", label: "Impressum" },
  { href: "/datenschutz", label: "Datenschutz" },
];

export function Footer() {
  return (
    <footer className="bg-navy text-white/50 mt-auto">
      <div className="max-w-6xl mx-auto px-4 sm:px-6 py-10">
        <div className="flex flex-col md:flex-row md:items-start md:justify-between gap-8">
          {/* Brand */}
          <div>
            <Link href="/" className="text-lg font-black tracking-tight">
              <span className="text-white">Flag</span>
              <span className="text-gold">Zone</span>
            </Link>
            <p className="mt-2 text-sm max-w-xs">
              Dein Einstieg in Flag Football — Regeln, Teams, Events & mehr.
            </p>
          </div>

          {/* Navigation */}
          <div className="flex gap-12">
            <div>
              <h4 className="text-white/70 text-xs font-bold uppercase tracking-wider mb-3">Inhalte</h4>
              <ul className="space-y-1.5">
                {FOOTER_LINKS.map((link) => (
                  <li key={link.href}>
                    <Link href={link.href} className="text-sm hover:text-gold transition-colors">
                      {link.label}
                    </Link>
                  </li>
                ))}
              </ul>
            </div>
            <div>
              <h4 className="text-white/70 text-xs font-bold uppercase tracking-wider mb-3">Rechtliches</h4>
              <ul className="space-y-1.5">
                {LEGAL_LINKS.map((link) => (
                  <li key={link.href}>
                    <Link href={link.href} className="text-sm hover:text-gold transition-colors">
                      {link.label}
                    </Link>
                  </li>
                ))}
              </ul>
            </div>
          </div>
        </div>

        <div className="mt-10 pt-6 border-t border-white/10 text-xs text-center">
          &copy; {new Date().getFullYear()} FlagZone. Made with 🏈 in Deutschland.
        </div>
      </div>
    </footer>
  );
}
```

- [ ] **Step 3: Update root layout**

Replace `src/app/layout.tsx`:

```tsx
import type { Metadata } from "next";
import { Nav } from "@/components/Nav";
import { Footer } from "@/components/Footer";
import "./globals.css";

export const metadata: Metadata = {
  metadataBase: new URL("https://flagzone.de"),
  title: {
    default: "FlagZone — Dein Einstieg in Flag Football",
    template: "%s | FlagZone",
  },
  description:
    "Alles rund um Flag Football in Deutschland: Regeln, Teams, Events, Olympia 2028 und die WM 2026 in Düsseldorf.",
  openGraph: {
    type: "website",
    locale: "de_DE",
    siteName: "FlagZone",
  },
  twitter: {
    card: "summary_large_image",
  },
  alternates: {
    canonical: "https://flagzone.de",
  },
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="de">
      <body className="min-h-screen flex flex-col">
        <Nav />
        {children}
        <Footer />
      </body>
    </html>
  );
}
```

- [ ] **Step 4: Verify nav and footer render**

```bash
npm run dev
```

Expected: Page shows sticky navy nav with FlagZone logo and links, content area, and navy footer with navigation columns. Mobile hamburger works at narrow widths. Stop server.

- [ ] **Step 5: Commit**

```bash
git add src/components/Nav.tsx src/components/Footer.tsx src/app/layout.tsx
git commit -m "feat: add Nav and Footer with responsive mobile menu"
```

---

## Task 3: JSON-LD Structured Data Helper

**Files:**
- Create: `src/components/JsonLd.tsx`

- [ ] **Step 1: Create JsonLd component**

Create `src/components/JsonLd.tsx`:

```tsx
type JsonLdProps = {
  data: Record<string, unknown>;
};

export function JsonLd({ data }: JsonLdProps) {
  return (
    <script
      type="application/ld+json"
      dangerouslySetInnerHTML={{ __html: JSON.stringify(data) }}
    />
  );
}
```

- [ ] **Step 2: Commit**

```bash
git add src/components/JsonLd.tsx
git commit -m "feat: add JSON-LD structured data component"
```

---

## Task 4: Sitemap & Robots

**Files:**
- Create: `src/app/sitemap.ts`
- Create: `src/app/robots.ts`

- [ ] **Step 1: Create dynamic sitemap**

Create `src/app/sitemap.ts`:

```typescript
import type { MetadataRoute } from "next";

const BASE_URL = "https://flagzone.de";

export default function sitemap(): MetadataRoute.Sitemap {
  const pages = [
    { path: "/", priority: 1.0, changeFrequency: "weekly" as const },
    { path: "/was-ist-flag-football", priority: 0.9, changeFrequency: "monthly" as const },
    { path: "/olympia-2028", priority: 0.9, changeFrequency: "monthly" as const },
    { path: "/wm-2026-duesseldorf", priority: 0.9, changeFrequency: "weekly" as const },
    { path: "/ausruestung", priority: 0.8, changeFrequency: "monthly" as const },
    { path: "/team-finden", priority: 0.8, changeFrequency: "monthly" as const },
    { path: "/impressum", priority: 0.2, changeFrequency: "yearly" as const },
    { path: "/datenschutz", priority: 0.2, changeFrequency: "yearly" as const },
  ];

  return pages.map((page) => ({
    url: `${BASE_URL}${page.path}`,
    lastModified: new Date(),
    changeFrequency: page.changeFrequency,
    priority: page.priority,
  }));
}
```

- [ ] **Step 2: Create robots.ts**

Create `src/app/robots.ts`:

```typescript
import type { MetadataRoute } from "next";

export default function robots(): MetadataRoute.Robots {
  return {
    rules: {
      userAgent: "*",
      allow: "/",
    },
    sitemap: "https://flagzone.de/sitemap.xml",
  };
}
```

- [ ] **Step 3: Verify sitemap and robots generate**

```bash
npm run build
```

Expected: Build succeeds. Check `.next/server/app/sitemap.xml` and `.next/server/app/robots.txt` exist in build output (they're generated at build time).

- [ ] **Step 4: Commit**

```bash
git add src/app/sitemap.ts src/app/robots.ts
git commit -m "feat: add dynamic sitemap and robots.txt"
```

---

## Task 5: Data Files — Teams & Events

**Files:**
- Create: `src/data/teams.json`
- Create: `src/data/events.json`

- [ ] **Step 1: Create teams.json with initial dataset**

Create `src/data/teams.json`:

```json
[
  {
    "name": "Düsseldorf Firecats",
    "city": "Düsseldorf",
    "state": "NRW",
    "lat": 51.2277,
    "lng": 6.7735,
    "league": "DFFL",
    "website": "https://www.duesseldorf-firecats.de",
    "gender": ["women"]
  },
  {
    "name": "Walldorf Wanderers",
    "city": "Walldorf",
    "state": "BW",
    "lat": 49.3064,
    "lng": 8.6425,
    "league": "DFFL",
    "website": "",
    "gender": ["men"]
  },
  {
    "name": "Augsburg Lions",
    "city": "Augsburg",
    "state": "BY",
    "lat": 48.3668,
    "lng": 10.8986,
    "league": "DFFL",
    "website": "",
    "gender": ["women"]
  },
  {
    "name": "Mainz Legionaries",
    "city": "Mainz",
    "state": "RP",
    "lat": 49.9929,
    "lng": 8.2473,
    "league": "DFFL",
    "website": "",
    "gender": ["men"]
  },
  {
    "name": "Berlin Adler",
    "city": "Berlin",
    "state": "BE",
    "lat": 52.5200,
    "lng": 13.4050,
    "league": "DFFL2",
    "website": "",
    "gender": ["men"]
  },
  {
    "name": "Hamburg Huskies",
    "city": "Hamburg",
    "state": "HH",
    "lat": 53.5511,
    "lng": 9.9937,
    "league": "DFFL2",
    "website": "",
    "gender": ["men", "women"]
  },
  {
    "name": "München Rangers",
    "city": "München",
    "state": "BY",
    "lat": 48.1351,
    "lng": 11.5820,
    "league": "DFFL",
    "website": "",
    "gender": ["men"]
  },
  {
    "name": "Frankfurt Universe Flag",
    "city": "Frankfurt am Main",
    "state": "HE",
    "lat": 50.1109,
    "lng": 8.6821,
    "league": "DFFL",
    "website": "",
    "gender": ["men", "women"]
  },
  {
    "name": "Cologne Centurions Flag",
    "city": "Köln",
    "state": "NRW",
    "lat": 50.9375,
    "lng": 6.9603,
    "league": "DFFL",
    "website": "",
    "gender": ["men"]
  },
  {
    "name": "Stuttgart Scorpions",
    "city": "Stuttgart",
    "state": "BW",
    "lat": 48.7758,
    "lng": 9.1829,
    "league": "DFFL2",
    "website": "",
    "gender": ["men"]
  }
]
```

- [ ] **Step 2: Create events.json**

Create `src/data/events.json`:

```json
[
  {
    "id": "finals-2026",
    "title": "Die Finals — Deutsche Meisterschaft",
    "dateStart": "2026-07-23",
    "dateEnd": "2026-07-24",
    "city": "Hannover",
    "description": "DFFL & DFFLF Finale bei den Finals 2026",
    "type": "national"
  },
  {
    "id": "wm-2026",
    "title": "IFAF Flag Football WM 2026",
    "dateStart": "2026-08-13",
    "dateEnd": "2026-08-16",
    "city": "Düsseldorf",
    "description": "32 Teams, 19 Nationen — die letzte große Bühne vor Olympia",
    "type": "international"
  },
  {
    "id": "olympia-2028",
    "title": "Olympische Spiele Los Angeles",
    "dateStart": "2028-07-14",
    "dateEnd": "2028-07-30",
    "city": "Los Angeles",
    "description": "Flag Football Premiere — Herren & Damen",
    "type": "international"
  }
]
```

- [ ] **Step 3: Commit**

```bash
git add src/data/teams.json src/data/events.json
git commit -m "feat: add initial teams and events data"
```

---

## Task 6: Startseite (Homepage)

**Files:**
- Modify: `src/app/page.tsx`

This is the most design-intensive page. It must NOT look like a generic template. Key layout decisions:
- Hero with oversized headline, asymmetric layout, gradient
- Stats bar — dense, tight, editorial feel
- Content section — mixed card sizes (feature card + smaller cards), not equal grid
- Events — dark section, timeline-style, not cards-in-a-row
- Article teasers — magazine-style horizontal layout with large/small alternation

- [ ] **Step 1: Build the homepage**

Replace `src/app/page.tsx`:

```tsx
import Link from "next/link";
import { JsonLd } from "@/components/JsonLd";
import eventsData from "@/data/events.json";

export default function Home() {
  return (
    <main>
      <JsonLd
        data={{
          "@context": "https://schema.org",
          "@type": "WebSite",
          name: "FlagZone",
          url: "https://flagzone.de",
          description: "Dein Einstieg in Flag Football — Regeln, Teams, Events & mehr.",
          inLanguage: "de-DE",
        }}
      />

      {/* ===== HERO — asymmetric, bold ===== */}
      <section className="bg-navy relative overflow-hidden">
        <div className="absolute top-0 right-0 w-[600px] h-[600px] bg-gold/10 rounded-full blur-3xl -translate-y-1/2 translate-x-1/4" />
        <div className="absolute bottom-0 left-0 w-[300px] h-[300px] bg-gold/5 rounded-full blur-2xl translate-y-1/2 -translate-x-1/4" />

        <div className="max-w-6xl mx-auto px-4 sm:px-6 py-16 md:py-24 relative">
          <div className="max-w-3xl">
            <span className="inline-block text-gold text-xs font-bold uppercase tracking-widest mb-4 border border-gold/30 px-3 py-1 rounded-full">
              Ab 2028 olympisch
            </span>
            <h1 className="text-white text-5xl sm:text-6xl md:text-7xl font-black leading-[0.95] tracking-tight mb-6">
              Flag Football
              <br />
              in Deutschland.
              <span className="block text-gold mt-2">Einfach loslegen.</span>
            </h1>
            <p className="text-white/60 text-lg md:text-xl max-w-xl mb-8 leading-relaxed">
              Regeln, Teams, Events und der Weg zu Olympia 2028.
              Dein Einstieg beginnt hier.
            </p>
            <div className="flex flex-wrap gap-3">
              <Link
                href="/was-ist-flag-football"
                className="bg-gold text-navy font-bold px-6 py-3 rounded-lg hover:bg-gold-hover transition-colors"
              >
                Was ist Flag Football?
              </Link>
              <Link
                href="/team-finden"
                className="border border-white/25 text-white/80 font-semibold px-6 py-3 rounded-lg hover:border-gold hover:text-white transition-colors"
              >
                Team in deiner Nähe
              </Link>
            </div>
          </div>
        </div>
      </section>

      {/* ===== STATS — dense, editorial ===== */}
      <section className="bg-white border-b border-zinc-200">
        <div className="max-w-6xl mx-auto px-4 sm:px-6 py-5 flex justify-between items-center overflow-x-auto gap-8 text-center">
          {[
            { number: "500+", label: "Vereine" },
            { number: "2028", label: "Olympia-Premiere" },
            { number: "20 Mio", label: "NFL-Fans in DE" },
            { number: "100+", label: "Länder weltweit" },
          ].map((stat) => (
            <div key={stat.label} className="min-w-fit">
              <div className="text-2xl md:text-3xl font-black text-navy tracking-tight">{stat.number}</div>
              <div className="text-[11px] text-muted font-semibold uppercase tracking-wider mt-0.5">{stat.label}</div>
            </div>
          ))}
        </div>
      </section>

      {/* ===== CONTENT — mixed card sizes, not equal grid ===== */}
      <section className="max-w-6xl mx-auto px-4 sm:px-6 py-16 md:py-20">
        <p className="text-gold font-bold text-xs uppercase tracking-widest mb-2">Einsteiger</p>
        <h2 className="text-navy text-3xl md:text-4xl font-black tracking-tight mb-3">Dein Weg auf den Platz</h2>
        <p className="text-muted text-lg mb-10 max-w-2xl">
          Du hast von Flag Football gehört und willst wissen, was dahinter steckt?
          Hier findest du alles, um loszulegen.
        </p>

        {/* Feature card (large) + two smaller cards */}
        <div className="grid md:grid-cols-5 gap-4">
          {/* Large featured card — spans 3 cols */}
          <Link
            href="/was-ist-flag-football"
            className="md:col-span-3 group bg-navy rounded-xl p-8 md:p-10 relative overflow-hidden hover:shadow-2xl transition-shadow"
          >
            <div className="absolute top-0 right-0 w-48 h-48 bg-gold/10 rounded-full blur-2xl" />
            <span className="text-gold text-xs font-bold uppercase tracking-widest">Regeln</span>
            <h3 className="text-white text-2xl md:text-3xl font-black mt-3 mb-3 group-hover:text-gold transition-colors">
              Was ist Flag Football?
            </h3>
            <p className="text-white/60 text-base max-w-md mb-6">
              5 gegen 5, kein Tackling, kein teures Equipment — der schnellste Einstieg
              in American Football. Wir erklären die Regeln.
            </p>
            <span className="text-gold font-semibold text-sm group-hover:underline">
              Regeln lernen →
            </span>
          </Link>

          {/* Two stacked smaller cards — span 2 cols */}
          <div className="md:col-span-2 flex flex-col gap-4">
            <Link
              href="/olympia-2028"
              className="group flex-1 bg-white border border-zinc-200 rounded-xl p-6 hover:border-gold transition-colors"
            >
              <span className="text-gold text-xs font-bold uppercase tracking-widest">Olympia</span>
              <h3 className="text-navy text-lg font-bold mt-2 mb-2 group-hover:text-gold transition-colors">
                Flag Football bei Olympia 2028
              </h3>
              <p className="text-muted text-sm">
                Erstmals olympisch in Los Angeles. Was das für den Sport bedeutet.
              </p>
            </Link>
            <Link
              href="/team-finden"
              className="group flex-1 bg-white border border-zinc-200 rounded-xl p-6 hover:border-gold transition-colors"
            >
              <span className="text-gold text-xs font-bold uppercase tracking-widest">Community</span>
              <h3 className="text-navy text-lg font-bold mt-2 mb-2 group-hover:text-gold transition-colors">
                Team in deiner Nähe finden
              </h3>
              <p className="text-muted text-sm">
                Über 500 Vereine in ganz Deutschland. Finde dein Team.
              </p>
            </Link>
          </div>
        </div>
      </section>

      {/* ===== EVENTS — dark, timeline-style ===== */}
      <section className="bg-navy">
        <div className="max-w-6xl mx-auto px-4 sm:px-6 py-16 md:py-20">
          <div className="md:grid md:grid-cols-2 md:gap-16 md:items-start">
            <div>
              <p className="text-gold font-bold text-xs uppercase tracking-widest mb-2">Events</p>
              <h2 className="text-white text-3xl md:text-4xl font-black tracking-tight mb-4">
                2026 wird das Jahr
                <span className="block text-gold">des Flag Footballs</span>
              </h2>
              <p className="text-white/50 text-base mb-6 max-w-md">
                Die WM in Düsseldorf, die Deutsche Meisterschaft in Hannover — noch nie
                hatte Flag Football in Deutschland so viel Aufmerksamkeit.
              </p>
            </div>

            <div className="space-y-3">
              {eventsData.map((event) => {
                const start = new Date(event.dateStart);
                const end = new Date(event.dateEnd);
                const dateStr =
                  start.getFullYear() === end.getFullYear() &&
                  start.getMonth() === end.getMonth()
                    ? `${start.getDate()}.–${end.getDate()}. ${start.toLocaleDateString("de-DE", { month: "long", year: "numeric" })}`
                    : start.toLocaleDateString("de-DE", { day: "numeric", month: "long", year: "numeric" });

                return (
                  <div
                    key={event.id}
                    className="bg-white/5 border border-white/10 rounded-lg p-5 hover:border-gold/40 transition-colors"
                  >
                    <div className="text-gold text-xs font-bold uppercase tracking-wider mb-1">{dateStr}</div>
                    <div className="text-white font-bold text-lg">{event.title}</div>
                    <div className="text-white/40 text-sm mt-0.5">{event.city} — {event.description}</div>
                  </div>
                );
              })}
            </div>
          </div>
        </div>
      </section>

      {/* ===== ARTICLE TEASERS — magazine-style ===== */}
      <section className="max-w-6xl mx-auto px-4 sm:px-6 py-16 md:py-20">
        <p className="text-gold font-bold text-xs uppercase tracking-widest mb-2">Wissen</p>
        <h2 className="text-navy text-3xl md:text-4xl font-black tracking-tight mb-10">Aktuelles & Guides</h2>

        <div className="space-y-4">
          {[
            {
              tag: "Regeln",
              title: "Flag Football Regeln einfach erklärt — der komplette Guide",
              desc: "Spielfeld, Positionen, Downs, Scoring — alles was du wissen musst, verständlich aufbereitet.",
              href: "/was-ist-flag-football",
              time: "8 Min.",
            },
            {
              tag: "WM 2026",
              title: "WM in Düsseldorf: Das musst du über die Weltmeisterschaft wissen",
              desc: "32 Teams, die letzte große Bühne vor Olympia — und Deutschland ist mittendrin.",
              href: "/wm-2026-duesseldorf",
              time: "5 Min.",
            },
            {
              tag: "Equipment",
              title: "Flag Football Ausrüstung — was brauchst du wirklich?",
              desc: "Flag-Set, Schuhe, Handschuhe? Was du zum Spielen brauchst und wo du es kaufst.",
              href: "/ausruestung",
              time: "4 Min.",
            },
          ].map((article) => (
            <Link
              key={article.href}
              href={article.href}
              className="group flex gap-6 bg-bg-warm rounded-xl p-5 hover:shadow-md transition-shadow"
            >
              {/* Placeholder thumbnail */}
              <div className="hidden sm:flex items-center justify-center min-w-[160px] h-[110px] bg-gradient-to-br from-navy to-navy-light rounded-lg text-3xl shrink-0">
                🏈
              </div>
              <div className="flex-1 min-w-0">
                <span className="text-gold-hover text-[11px] font-bold uppercase tracking-wider">{article.tag}</span>
                <h3 className="text-navy font-bold text-base md:text-lg mt-1 mb-1 group-hover:text-gold-hover transition-colors">
                  {article.title}
                </h3>
                <p className="text-muted text-sm line-clamp-2">{article.desc}</p>
                <span className="text-muted/60 text-xs mt-2 block">{article.time} Lesezeit</span>
              </div>
            </Link>
          ))}
        </div>
      </section>
    </main>
  );
}
```

- [ ] **Step 2: Verify homepage renders**

```bash
npm run dev
```

Expected: Full homepage with hero, stats bar, mixed-size content cards, dark events section, and article teasers. Check mobile responsiveness at narrow viewport. Stop server.

- [ ] **Step 3: Commit**

```bash
git add src/app/page.tsx
git commit -m "feat: build Startseite with editorial layout"
```

---

## Task 7: Page — Was ist Flag Football?

**Files:**
- Create: `src/app/was-ist-flag-football/page.tsx`

Long-form editorial page with varied sections: intro, field diagram (CSS-drawn), positions, rules breakdown, FAQ with JSON-LD. Opinionated layout — not a wall of text.

- [ ] **Step 1: Create the rules page**

Create `src/app/was-ist-flag-football/page.tsx`:

```tsx
import type { Metadata } from "next";
import Link from "next/link";
import { JsonLd } from "@/components/JsonLd";

export const metadata: Metadata = {
  title: "Was ist Flag Football? Regeln einfach erklärt",
  description:
    "Flag Football Regeln, Positionen, Spielfeld und Scoring — einfach erklärt. Der komplette Guide für Einsteiger. 5 gegen 5, kein Tackling, einfach loslegen.",
  alternates: { canonical: "https://flagzone.de/was-ist-flag-football" },
};

const FAQ_ITEMS = [
  {
    q: "Wie viele Spieler sind auf dem Feld?",
    a: "5 gegen 5. Jedes Team hat einen Kader von 10 Spielern, aber nur 5 stehen gleichzeitig auf dem Feld.",
  },
  {
    q: "Brauche ich teure Ausrüstung?",
    a: "Nein. Ein Flag-Belt-Set und Sportschuhe reichen. Kein Helm, keine Pads, keine Schutzausrüstung nötig.",
  },
  {
    q: "Ist Flag Football gefährlich?",
    a: "Flag Football ist eine Nicht-Kontakt-Sportart. Tackling, Blocking und harter Körperkontakt sind verboten. Das Verletzungsrisiko ist deutlich geringer als bei Tackle Football.",
  },
  {
    q: "Ab welchem Alter kann man spielen?",
    a: "Flag Football wird ab ca. 6 Jahren angeboten. Es gibt Ligen für Kinder, Jugendliche und Erwachsene — und es ist gemischtgeschlechtlich (Co-Ed) möglich.",
  },
  {
    q: "Ist Flag Football olympisch?",
    a: "Ja! Flag Football feiert bei den Olympischen Spielen 2028 in Los Angeles seine Premiere — als erste Form von American Football überhaupt.",
  },
];

export default function WasIstFlagFootball() {
  return (
    <main>
      <JsonLd
        data={{
          "@context": "https://schema.org",
          "@type": "FAQPage",
          mainEntity: FAQ_ITEMS.map((item) => ({
            "@type": "Question",
            name: item.q,
            acceptedAnswer: { "@type": "Answer", text: item.a },
          })),
        }}
      />

      {/* ===== Hero — editorial, left-aligned ===== */}
      <section className="bg-navy">
        <div className="max-w-6xl mx-auto px-4 sm:px-6 py-14 md:py-20">
          <p className="text-gold text-xs font-bold uppercase tracking-widest mb-3">Regeln & Grundlagen</p>
          <h1 className="text-white text-4xl sm:text-5xl md:text-6xl font-black leading-[0.95] tracking-tight max-w-2xl">
            Was ist Flag Football?
          </h1>
          <p className="text-white/50 text-lg mt-4 max-w-xl">
            Kein Tackling, kein teures Equipment, sofort loslegen — der schnellste Einstieg in American Football.
          </p>
        </div>
      </section>

      <article className="max-w-6xl mx-auto px-4 sm:px-6 py-12 md:py-16">

        {/* ===== Intro — big text block ===== */}
        <div className="max-w-3xl mb-16">
          <p className="text-lg md:text-xl text-[#18181b] leading-relaxed mb-6">
            Flag Football ist die kontaktlose Variante von American Football. Statt den Ballträger
            zu tacklen, zieht die Verteidigung eine Flagge von seinem Gürtel — daher der Name.
            Gespielt wird <strong>5 gegen 5</strong> auf einem kleineren Feld, ohne Pads und Helme.
          </p>
          <p className="text-lg text-muted leading-relaxed">
            Der Sport wächst weltweit rasant: Über 20 Millionen Menschen in mehr als 100 Ländern spielen
            Flag Football. Ab 2028 ist es <Link href="/olympia-2028" className="text-navy font-semibold underline decoration-gold decoration-2 underline-offset-2 hover:text-gold-hover">erstmals olympisch</Link>.
            Deutschland ist amtierender Europameister und richtet 2026 die{" "}
            <Link href="/wm-2026-duesseldorf" className="text-navy font-semibold underline decoration-gold decoration-2 underline-offset-2 hover:text-gold-hover">Weltmeisterschaft in Düsseldorf</Link> aus.
          </p>
        </div>

        {/* ===== Key facts — pull-quote style ===== */}
        <div className="grid sm:grid-cols-3 gap-6 mb-16">
          {[
            { number: "5v5", label: "Spieler pro Team auf dem Feld" },
            { number: "70×25", label: "Yard — Spielfeldgröße" },
            { number: "2×20", label: "Minuten Spielzeit" },
          ].map((fact) => (
            <div key={fact.label} className="border-l-4 border-gold pl-5 py-2">
              <div className="text-navy text-4xl font-black tracking-tight">{fact.number}</div>
              <div className="text-muted text-sm mt-1">{fact.label}</div>
            </div>
          ))}
        </div>

        {/* ===== Spielfeld ===== */}
        <section className="mb-16">
          <h2 className="text-navy text-2xl md:text-3xl font-black tracking-tight mb-6">Das Spielfeld</h2>
          {/* CSS-drawn field diagram */}
          <div className="bg-emerald-700 rounded-xl p-6 md:p-8 relative overflow-hidden mb-6">
            <div className="flex">
              {/* End zone left */}
              <div className="w-[14%] border-2 border-dashed border-white/40 rounded-l-lg flex items-center justify-center min-h-[140px] md:min-h-[180px]">
                <span className="text-white/60 text-xs font-bold uppercase tracking-wider [writing-mode:vertical-lr] rotate-180">Endzone</span>
              </div>
              {/* Field body */}
              <div className="flex-1 border-y-2 border-white/40 relative">
                {/* Midfield line */}
                <div className="absolute left-1/2 top-0 bottom-0 border-l-2 border-dashed border-white/40" />
                {/* No-run zones */}
                <div className="absolute left-[8%] top-0 bottom-0 w-[12%] bg-white/10" />
                <div className="absolute right-[8%] top-0 bottom-0 w-[12%] bg-white/10" />
                {/* Labels */}
                <div className="flex items-center justify-center h-full min-h-[140px] md:min-h-[180px]">
                  <div className="text-center">
                    <div className="text-white font-bold text-base md:text-lg">70 Yards</div>
                    <div className="text-white/40 text-xs mt-1">+ 2×10 Yard Endzones</div>
                  </div>
                </div>
              </div>
              {/* End zone right */}
              <div className="w-[14%] border-2 border-dashed border-white/40 rounded-r-lg flex items-center justify-center">
                <span className="text-white/60 text-xs font-bold uppercase tracking-wider [writing-mode:vertical-lr]">Endzone</span>
              </div>
            </div>
            {/* Width label */}
            <div className="text-center text-white/40 text-xs mt-3">25 Yards breit</div>
          </div>
          <p className="text-muted text-base leading-relaxed max-w-3xl">
            Das Spielfeld ist 70 Yards lang und 25 Yards breit — deutlich kleiner als ein Tackle-Football-Feld.
            An beiden Enden liegen die Endzones (je 10 Yards). Die helleren Bereiche markieren die No-Run-Zones
            vor den Endzones, in denen nicht gelaufen werden darf.
          </p>
        </section>

        {/* ===== Positionen ===== */}
        <section className="mb-16">
          <h2 className="text-navy text-2xl md:text-3xl font-black tracking-tight mb-6">Die Positionen</h2>
          <div className="grid sm:grid-cols-2 gap-4">
            {[
              {
                title: "Quarterback (QB)",
                desc: "Dirigiert den Angriff und wirft den Ball. Hat 7 Sekunden für den Wurf.",
                side: "Offense",
              },
              {
                title: "Center (C)",
                desc: "Snapt den Ball zum QB und läuft dann eine Route als Receiver.",
                side: "Offense",
              },
              {
                title: "Wide Receiver (WR)",
                desc: "Läuft Routen und fängt Pässe. Geschwindigkeit und Wendigkeit sind entscheidend.",
                side: "Offense",
              },
              {
                title: "Rusher",
                desc: "Versucht den QB unter Druck zu setzen. Muss 7 Yards Abstand halten und sich vor dem Snap identifizieren.",
                side: "Defense",
              },
              {
                title: "Defensive Back (DB)",
                desc: "Deckt die Receiver und versucht, dem Ballträger die Flagge zu ziehen.",
                side: "Defense",
              },
            ].map((pos) => (
              <div
                key={pos.title}
                className="bg-bg-warm rounded-lg p-5 border-l-4"
                style={{ borderLeftColor: pos.side === "Offense" ? "#FFC72C" : "#002B5C" }}
              >
                <div className="flex items-center gap-2 mb-1">
                  <span className="text-[10px] font-bold uppercase tracking-wider text-muted">{pos.side}</span>
                </div>
                <h3 className="text-navy font-bold text-base">{pos.title}</h3>
                <p className="text-muted text-sm mt-1">{pos.desc}</p>
              </div>
            ))}
          </div>
        </section>

        {/* ===== Regeln / Ablauf ===== */}
        <section className="mb-16">
          <h2 className="text-navy text-2xl md:text-3xl font-black tracking-tight mb-6">Spielablauf & Regeln</h2>
          <div className="max-w-3xl space-y-6">
            <div>
              <h3 className="text-navy font-bold text-lg mb-2">Downs</h3>
              <p className="text-muted leading-relaxed">
                Die Offense hat <strong>4 Versuche (Downs)</strong>, um die Mittellinie zu überqueren.
                Schafft sie das, bekommt sie 4 neue Versuche für den Touchdown. Schafft sie es nicht,
                wechselt der Ballbesitz.
              </p>
            </div>
            <div>
              <h3 className="text-navy font-bold text-lg mb-2">Scoring</h3>
              <p className="text-muted leading-relaxed">
                <strong>Touchdown = 6 Punkte.</strong> Danach gibt es einen Extrapunkt-Versuch:
                1 Punkt von der 5-Yard-Linie oder 2 Punkte von der 10-Yard-Linie.
                Fängt die Defense einen Pass ab und trägt ihn in die Endzone, gibt es ebenfalls Punkte.
              </p>
            </div>
            <div>
              <h3 className="text-navy font-bold text-lg mb-2">Kein Kontakt</h3>
              <p className="text-muted leading-relaxed">
                Tackling, Blocking und Körperkontakt sind verboten. Der Ballträger wird gestoppt,
                indem eine Flagge von seinem Gürtel gezogen wird. Der QB darf nicht laufen (außer in bestimmten Spielzügen).
              </p>
            </div>
            <div>
              <h3 className="text-navy font-bold text-lg mb-2">7-Sekunden-Regel</h3>
              <p className="text-muted leading-relaxed">
                Der Quarterback hat <strong>7 Sekunden</strong> nach dem Snap, um den Ball zu werfen.
                Schafft er es nicht, gilt der Spielzug als Sack (Verlust).
              </p>
            </div>
          </div>
        </section>

        {/* ===== FAQ ===== */}
        <section className="mb-16">
          <h2 className="text-navy text-2xl md:text-3xl font-black tracking-tight mb-6">Häufige Fragen</h2>
          <div className="max-w-3xl divide-y divide-zinc-200">
            {FAQ_ITEMS.map((item) => (
              <details key={item.q} className="py-4 group">
                <summary className="flex items-center justify-between cursor-pointer list-none">
                  <span className="text-navy font-semibold text-base pr-4">{item.q}</span>
                  <span className="text-muted text-xl shrink-0 group-open:rotate-45 transition-transform">+</span>
                </summary>
                <p className="text-muted text-sm mt-3 leading-relaxed">{item.a}</p>
              </details>
            ))}
          </div>
        </section>

        {/* ===== CTA ===== */}
        <section className="bg-navy rounded-xl p-8 md:p-12 text-center">
          <h2 className="text-white text-2xl md:text-3xl font-black mb-3">Bereit loszulegen?</h2>
          <p className="text-white/50 mb-6 max-w-md mx-auto">
            Finde ein Team in deiner Nähe oder schau dir an, welche Ausrüstung du brauchst.
          </p>
          <div className="flex flex-wrap justify-center gap-3">
            <Link
              href="/team-finden"
              className="bg-gold text-navy font-bold px-6 py-3 rounded-lg hover:bg-gold-hover transition-colors"
            >
              Team finden
            </Link>
            <Link
              href="/ausruestung"
              className="border border-white/25 text-white/80 font-semibold px-6 py-3 rounded-lg hover:border-gold hover:text-white transition-colors"
            >
              Equipment-Guide
            </Link>
          </div>
        </section>
      </article>
    </main>
  );
}
```

- [ ] **Step 2: Verify the page renders**

```bash
npm run dev
```

Navigate to http://localhost:3000/was-ist-flag-football. Expected: Hero, intro text, key facts, field diagram, positions grid, rules, FAQ with expand/collapse, and CTA. Stop server.

- [ ] **Step 3: Commit**

```bash
git add src/app/was-ist-flag-football/page.tsx
git commit -m "feat: add Was ist Flag Football page with rules, FAQ, field diagram"
```

---

## Task 8: Page — WM 2026 Düsseldorf

**Files:**
- Create: `src/app/wm-2026-duesseldorf/page.tsx`

- [ ] **Step 1: Create the WM page**

Create `src/app/wm-2026-duesseldorf/page.tsx`:

```tsx
import type { Metadata } from "next";
import Link from "next/link";
import { JsonLd } from "@/components/JsonLd";

export const metadata: Metadata = {
  title: "Flag Football WM 2026 in Düsseldorf — Alles zur Weltmeisterschaft",
  description:
    "Die IFAF Flag Football Weltmeisterschaft 2026 findet in Düsseldorf statt. 32 Teams, 19 Nationen — die letzte große Bühne vor Olympia 2028.",
  alternates: { canonical: "https://flagzone.de/wm-2026-duesseldorf" },
};

export default function WM2026() {
  return (
    <main>
      <JsonLd
        data={{
          "@context": "https://schema.org",
          "@type": "SportsEvent",
          name: "IFAF Flag Football Weltmeisterschaft 2026",
          startDate: "2026-08-13",
          endDate: "2026-08-16",
          location: {
            "@type": "Place",
            name: "Bezirkssportanlage Koblenzer Straße",
            address: {
              "@type": "PostalAddress",
              addressLocality: "Düsseldorf",
              addressCountry: "DE",
            },
          },
          description: "32 Teams aus 19 Nationen kämpfen um den Weltmeistertitel im Flag Football.",
        }}
      />

      {/* Hero */}
      <section className="bg-navy">
        <div className="max-w-6xl mx-auto px-4 sm:px-6 py-14 md:py-20">
          <p className="text-gold text-xs font-bold uppercase tracking-widest mb-3">13.–16. August 2026</p>
          <h1 className="text-white text-4xl sm:text-5xl md:text-6xl font-black leading-[0.95] tracking-tight max-w-3xl">
            Flag Football WM 2026
            <span className="block text-gold">in Düsseldorf</span>
          </h1>
          <p className="text-white/50 text-lg mt-4 max-w-xl">
            32 Teams, 19 Nationen, eine Stadt — die letzte große Bühne vor der olympischen Premiere 2028.
          </p>
        </div>
      </section>

      <article className="max-w-6xl mx-auto px-4 sm:px-6 py-12 md:py-16">

        {/* Key facts */}
        <div className="grid sm:grid-cols-4 gap-6 mb-16">
          {[
            { number: "32", label: "Teams" },
            { number: "19", label: "Nationen" },
            { number: "16+16", label: "Herren & Damen" },
            { number: "4 Tage", label: "Turnierdauer" },
          ].map((fact) => (
            <div key={fact.label} className="border-l-4 border-gold pl-5 py-2">
              <div className="text-navy text-3xl font-black tracking-tight">{fact.number}</div>
              <div className="text-muted text-sm mt-1">{fact.label}</div>
            </div>
          ))}
        </div>

        {/* Content */}
        <div className="max-w-3xl space-y-8 mb-16">
          <section>
            <h2 className="text-navy text-2xl md:text-3xl font-black tracking-tight mb-4">Warum Düsseldorf?</h2>
            <p className="text-muted leading-relaxed mb-4">
              Düsseldorf hat sich in den letzten Jahren zum Zentrum des deutschen Flag Footballs entwickelt.
              Im April 2025 eröffneten die New England Patriots zusammen mit knapp 2.000 Gästen den
              ersten Purpose-Built Flag Football Komplex Deutschlands — die Bezirkssportanlage Koblenzer Straße.
              Genau hier findet die WM statt.
            </p>
            <p className="text-muted leading-relaxed">
              Sebastian Vollmer, gebürtiger Düsseldorfer und zweifacher Super-Bowl-Champion mit den Patriots,
              ist das Gesicht der Verbindung zwischen NFL und deutschem Flag Football. Er ist IFAF Global
              Ambassador und treibt die Entwicklung des Sports in seiner Heimatstadt voran.
            </p>
          </section>

          <section>
            <h2 className="text-navy text-2xl md:text-3xl font-black tracking-tight mb-4">Das Turnier</h2>
            <p className="text-muted leading-relaxed mb-4">
              Die IFAF Flag Football Weltmeisterschaft 2026 ist das letzte große internationale Turnier
              vor der <Link href="/olympia-2028" className="text-navy font-semibold underline decoration-gold decoration-2 underline-offset-2 hover:text-gold-hover">olympischen Premiere in Los Angeles 2028</Link>.
              16 Herren- und 16 Damen-Teams aus 5 Kontinenten kämpfen um den WM-Titel.
            </p>
            <p className="text-muted leading-relaxed">
              Deutschland geht als amtierender Europameister (2023, Herren) ins Turnier und spielt vor
              heimischem Publikum. Die Gruppen wurden im November 2025 ausgelost.
            </p>
          </section>

          <section>
            <h2 className="text-navy text-2xl md:text-3xl font-black tracking-tight mb-4">Zeitplan</h2>
            <div className="space-y-3">
              {[
                { date: "13. August", desc: "Gruppenphase Tag 1 — Vorrunde Herren & Damen" },
                { date: "14. August", desc: "Gruppenphase Tag 2 — letzte Vorrundenspiele" },
                { date: "15. August", desc: "Viertelfinale & Halbfinale" },
                { date: "16. August", desc: "Finale Damen & Finale Herren" },
              ].map((day) => (
                <div key={day.date} className="flex gap-4 items-start bg-bg-warm rounded-lg p-4">
                  <div className="text-navy font-bold text-sm min-w-[100px]">{day.date}</div>
                  <div className="text-muted text-sm">{day.desc}</div>
                </div>
              ))}
            </div>
          </section>

          <section>
            <h2 className="text-navy text-2xl md:text-3xl font-black tracking-tight mb-4">Venue</h2>
            <p className="text-muted leading-relaxed mb-4">
              <strong>Bezirkssportanlage Koblenzer Straße</strong>, Düsseldorf — der erste speziell
              für Flag Football gebaute Komplex in Deutschland. Eröffnet im April 2025 in Zusammenarbeit
              mit den New England Patriots.
            </p>
          </section>
        </div>

        {/* CTA */}
        <section className="bg-navy rounded-xl p-8 md:p-12 text-center">
          <h2 className="text-white text-2xl md:text-3xl font-black mb-3">Neu bei Flag Football?</h2>
          <p className="text-white/50 mb-6 max-w-md mx-auto">
            Erfahre, wie der Sport funktioniert und warum er gerade die Welt erobert.
          </p>
          <Link
            href="/was-ist-flag-football"
            className="bg-gold text-navy font-bold px-6 py-3 rounded-lg hover:bg-gold-hover transition-colors"
          >
            Was ist Flag Football?
          </Link>
        </section>
      </article>
    </main>
  );
}
```

- [ ] **Step 2: Verify**

```bash
npm run dev
```

Navigate to http://localhost:3000/wm-2026-duesseldorf. Expected: Hero with date, key facts, content sections, schedule, CTA. Stop server.

- [ ] **Step 3: Commit**

```bash
git add src/app/wm-2026-duesseldorf/page.tsx
git commit -m "feat: add WM 2026 Düsseldorf page"
```

---

## Task 9: Page — Olympia 2028

**Files:**
- Create: `src/app/olympia-2028/page.tsx`

- [ ] **Step 1: Create the Olympics page**

Create `src/app/olympia-2028/page.tsx`:

```tsx
import type { Metadata } from "next";
import Link from "next/link";
import { JsonLd } from "@/components/JsonLd";

export const metadata: Metadata = {
  title: "Flag Football bei Olympia 2028 — Alles zur olympischen Premiere",
  description:
    "Flag Football ist ab 2028 olympisch! Alles über die Premiere in Los Angeles: Format, Regeln, deutsche Chancen und was die Aufnahme für den Sport bedeutet.",
  alternates: { canonical: "https://flagzone.de/olympia-2028" },
};

export default function Olympia2028() {
  return (
    <main>
      <JsonLd
        data={{
          "@context": "https://schema.org",
          "@type": "SportsEvent",
          name: "Flag Football — Olympische Spiele 2028",
          startDate: "2028-07-14",
          endDate: "2028-07-30",
          location: {
            "@type": "Place",
            name: "Los Angeles",
            address: { "@type": "PostalAddress", addressCountry: "US" },
          },
          description: "Erstmals ist Flag Football als olympische Disziplin bei den Spielen in Los Angeles vertreten.",
        }}
      />

      {/* Hero */}
      <section className="bg-navy">
        <div className="max-w-6xl mx-auto px-4 sm:px-6 py-14 md:py-20">
          <p className="text-gold text-xs font-bold uppercase tracking-widest mb-3">Los Angeles 2028</p>
          <h1 className="text-white text-4xl sm:text-5xl md:text-6xl font-black leading-[0.95] tracking-tight max-w-3xl">
            Flag Football
            <span className="block text-gold">wird olympisch</span>
          </h1>
          <p className="text-white/50 text-lg mt-4 max-w-xl">
            Erstmals in der Geschichte ist American Football — als Flag Football — Teil der Olympischen Spiele.
          </p>
        </div>
      </section>

      <article className="max-w-6xl mx-auto px-4 sm:px-6 py-12 md:py-16">

        {/* Timeline */}
        <div className="mb-16">
          <h2 className="text-navy text-2xl md:text-3xl font-black tracking-tight mb-8">Der Weg zu Olympia</h2>
          <div className="space-y-0 relative">
            <div className="absolute left-[15px] top-4 bottom-4 w-0.5 bg-zinc-200 hidden sm:block" />
            {[
              { year: "2023", title: "IOC-Aufnahme beschlossen", desc: "Am 16. Oktober 2023 genehmigt die 141. IOC-Session in Mumbai die Aufnahme von Flag Football ins Programm der Olympischen Spiele 2028 in Los Angeles." },
              { year: "2025", title: "Qualifikation läuft", desc: "Über Kontinentalmeisterschaften und die Weltmeisterschaft 2026 qualifizieren sich die Nationen für das olympische Turnier." },
              { year: "2026", title: "WM in Düsseldorf", desc: "Die letzte große Standortbestimmung vor Olympia. 32 Teams, 19 Nationen — Deutschland spielt zuhause." },
              { year: "2028", title: "Olympische Premiere", desc: "6 Herren- und 6 Damen-Teams spielen in Los Angeles um Gold. NFL-Spieler dürfen teilnehmen." },
            ].map((item) => (
              <div key={item.year} className="flex gap-5 items-start relative pb-8">
                <div className="w-8 h-8 rounded-full bg-gold flex items-center justify-center shrink-0 text-navy text-xs font-black relative z-10">
                  {item.year.slice(2)}
                </div>
                <div>
                  <div className="text-navy font-bold text-lg">{item.year} — {item.title}</div>
                  <p className="text-muted text-sm mt-1">{item.desc}</p>
                </div>
              </div>
            ))}
          </div>
        </div>

        {/* Format */}
        <section className="mb-16">
          <h2 className="text-navy text-2xl md:text-3xl font-black tracking-tight mb-6">Das olympische Format</h2>
          <div className="grid sm:grid-cols-2 gap-6 mb-8">
            {[
              { label: "Teamgröße", value: "5 auf dem Feld, 10er-Kader" },
              { label: "Teilnehmer", value: "Je 6 Nationen (Herren & Damen)" },
              { label: "Spielzeit", value: "2 × 20 Minuten" },
              { label: "Regelwerk", value: "IFAF 5-on-5 (kontaktlos)" },
              { label: "Spielfeld", value: "70 × 25 Yards" },
              { label: "NFL-Spieler", value: "Dürfen teilnehmen (seit 2024 genehmigt)" },
            ].map((item) => (
              <div key={item.label} className="flex gap-3 items-start bg-bg-warm rounded-lg p-4">
                <div className="text-navy font-bold text-sm min-w-[120px]">{item.label}</div>
                <div className="text-muted text-sm">{item.value}</div>
              </div>
            ))}
          </div>
        </section>

        {/* Germany */}
        <section className="mb-16 max-w-3xl">
          <h2 className="text-navy text-2xl md:text-3xl font-black tracking-tight mb-4">Deutschlands Chancen</h2>
          <p className="text-muted leading-relaxed mb-4">
            Deutschland ist amtierender Europameister im Flag Football (2023, Herren). Das Team besiegte
            Österreich im Finale mit 36:28. Auch die Junioren holten 2024 den EM-Titel.
          </p>
          <p className="text-muted leading-relaxed mb-4">
            Mit über 500 Vereinen, einer professionellen Liga (DFFL) und der WM 2026 im eigenen Land
            hat Deutschland eine starke Basis. Die Aufnahme ins Programm von{" "}
            <Link href="/wm-2026-duesseldorf" className="text-navy font-semibold underline decoration-gold decoration-2 underline-offset-2 hover:text-gold-hover">
              Die Finals (ZDF/ARD)
            </Link>{" "}
            seit 2025 sorgt für wachsende Sichtbarkeit.
          </p>
          <p className="text-muted leading-relaxed">
            Ob Deutschland sich für Olympia 2028 qualifiziert, hängt von den Kontinental- und
            Weltmeisterschaftsergebnissen ab. Die Chancen stehen gut.
          </p>
        </section>

        {/* Difference */}
        <section className="mb-16 max-w-3xl">
          <h2 className="text-navy text-2xl md:text-3xl font-black tracking-tight mb-4">Warum Flag Football und nicht Tackle?</h2>
          <p className="text-muted leading-relaxed mb-4">
            Das IOC wählte Flag Football aus mehreren Gründen: Es ist kontaktlos (geringeres Verletzungsrisiko),
            braucht weniger Equipment, ist gemischtgeschlechtlich spielbar und deutlich zugänglicher als Tackle Football.
            Mit 5-gegen-5 auf kleinem Feld passt es ins olympische Format.
          </p>
          <p className="text-muted leading-relaxed">
            Für den American Football ist es ein historischer Moment: Erstmals überhaupt ist eine Form
            dieses Sports bei Olympia vertreten.
          </p>
        </section>

        {/* CTA */}
        <section className="bg-navy rounded-xl p-8 md:p-12 text-center">
          <h2 className="text-white text-2xl md:text-3xl font-black mb-3">Die WM 2026 kommt zuerst</h2>
          <p className="text-white/50 mb-6 max-w-md mx-auto">
            Bevor es nach LA geht, steht die Weltmeisterschaft in Düsseldorf an. Alle Infos zum Turnier.
          </p>
          <Link
            href="/wm-2026-duesseldorf"
            className="bg-gold text-navy font-bold px-6 py-3 rounded-lg hover:bg-gold-hover transition-colors"
          >
            WM 2026 in Düsseldorf
          </Link>
        </section>
      </article>
    </main>
  );
}
```

- [ ] **Step 2: Verify**

```bash
npm run dev
```

Navigate to http://localhost:3000/olympia-2028. Expected: Hero, timeline, format grid, Germany section, CTA. Stop server.

- [ ] **Step 3: Commit**

```bash
git add src/app/olympia-2028/page.tsx
git commit -m "feat: add Olympia 2028 page with timeline and format details"
```

---

## Task 10: Page — Ausrüstung (Equipment + Affiliate)

**Files:**
- Create: `src/app/ausruestung/page.tsx`

- [ ] **Step 1: Create the equipment page**

Create `src/app/ausruestung/page.tsx`:

```tsx
import type { Metadata } from "next";
import Link from "next/link";
import { JsonLd } from "@/components/JsonLd";

export const metadata: Metadata = {
  title: "Flag Football Ausrüstung — Was du wirklich brauchst",
  description:
    "Flag Football Equipment Guide: Flag-Belts, Footballs, Schuhe und mehr. Was du zum Spielen brauchst und wo du es am besten kaufst.",
  alternates: { canonical: "https://flagzone.de/ausruestung" },
};

export default function Ausruestung() {
  return (
    <main>
      <JsonLd
        data={{
          "@context": "https://schema.org",
          "@type": "Article",
          headline: "Flag Football Ausrüstung — Was du wirklich brauchst",
          description: "Kompletter Equipment Guide für Flag Football Einsteiger.",
          inLanguage: "de-DE",
        }}
      />

      {/* Hero */}
      <section className="bg-navy">
        <div className="max-w-6xl mx-auto px-4 sm:px-6 py-14 md:py-20">
          <p className="text-gold text-xs font-bold uppercase tracking-widest mb-3">Equipment Guide</p>
          <h1 className="text-white text-4xl sm:text-5xl md:text-6xl font-black leading-[0.95] tracking-tight max-w-3xl">
            Was brauchst du
            <span className="block text-gold">zum Spielen?</span>
          </h1>
          <p className="text-white/50 text-lg mt-4 max-w-xl">
            Spoiler: Nicht viel. Flag Football ist einer der zugänglichsten Teamsports überhaupt.
          </p>
        </div>
      </section>

      <article className="max-w-6xl mx-auto px-4 sm:px-6 py-12 md:py-16">

        {/* Intro */}
        <div className="max-w-3xl mb-12">
          <p className="text-lg text-muted leading-relaxed">
            Anders als Tackle Football brauchst du keine Pads, keinen Helm und keine teure Schutzausrüstung.
            Ein Flag-Belt-Set und Sportschuhe — mehr nicht. Hier ist alles, was du wissen musst.
          </p>
        </div>

        {/* Priority indicator */}
        <div className="flex gap-4 mb-8 text-xs font-bold uppercase tracking-wider">
          <span className="flex items-center gap-1.5">
            <span className="w-3 h-3 rounded-full bg-gold" /> Pflicht
          </span>
          <span className="flex items-center gap-1.5">
            <span className="w-3 h-3 rounded-full bg-navy" /> Empfohlen
          </span>
          <span className="flex items-center gap-1.5">
            <span className="w-3 h-3 rounded-full bg-zinc-300" /> Optional
          </span>
        </div>

        {/* Equipment items */}
        <div className="space-y-6 mb-16">
          {/* Flag Belts — PFLICHT */}
          <div className="border-l-4 border-gold bg-bg-warm rounded-r-xl p-6 md:p-8">
            <div className="flex items-start justify-between gap-4">
              <div>
                <span className="text-gold text-[10px] font-bold uppercase tracking-widest">Pflicht</span>
                <h2 className="text-navy text-xl md:text-2xl font-black mt-1 mb-2">Flag Belts</h2>
                <p className="text-muted leading-relaxed mb-3">
                  Das Herzstück des Spiels. Jeder Spieler trägt einen Gürtel mit zwei abnehmbaren Flaggen
                  (links und rechts). Die Verteidigung stoppt den Ballträger, indem sie eine Flagge zieht.
                </p>
                <p className="text-muted leading-relaxed mb-3">
                  Für ein Team (10 Spieler) brauchst du ein Set mit mindestens 10 Gürteln in zwei Farben.
                  Standard-Sets kosten <strong>25–50 EUR</strong> und reichen für den Einstieg.
                </p>
                <p className="text-sm text-muted/70">
                  <span className="font-semibold">Tipp:</span> Achte auf verstellbare Gürtel mit Klettverschluss-Flaggen —
                  die lösen sich sauber und sind ligakonform.
                </p>
              </div>
            </div>
            <p className="text-xs text-muted/50 mt-4">
              * Links auf dieser Seite können Affiliate-Links sein. Wenn du darüber kaufst, erhalten wir eine kleine Provision — für dich ändert sich nichts am Preis.
            </p>
          </div>

          {/* Football — PFLICHT */}
          <div className="border-l-4 border-gold bg-bg-warm rounded-r-xl p-6 md:p-8">
            <span className="text-gold text-[10px] font-bold uppercase tracking-widest">Pflicht</span>
            <h2 className="text-navy text-xl md:text-2xl font-black mt-1 mb-2">Football</h2>
            <p className="text-muted leading-relaxed mb-3">
              Für Flag Football wird ein regulärer American Football verwendet — idealerweise in
              der offiziellen Größe (Youth oder Official je nach Liga). Gummi-Footballs sind
              günstiger und für Einsteiger völlig ausreichend. Leder-Bälle bieten besseren Grip.
            </p>
            <p className="text-muted leading-relaxed">
              <strong>Preisrange:</strong> 15–40 EUR für einen guten Trainingsball.
            </p>
          </div>

          {/* Schuhe — EMPFOHLEN */}
          <div className="border-l-4 border-navy bg-bg-warm rounded-r-xl p-6 md:p-8">
            <span className="text-navy text-[10px] font-bold uppercase tracking-widest">Empfohlen</span>
            <h2 className="text-navy text-xl md:text-2xl font-black mt-1 mb-2">Schuhe</h2>
            <p className="text-muted leading-relaxed mb-3">
              Auf Rasen spielst du am besten mit Stollenschuhen (Fußball-Schuhe funktionieren
              perfekt). Auf Kunstrasen oder in der Halle reichen Hallenschuhe oder Turf-Schuhe.
            </p>
            <p className="text-muted leading-relaxed">
              <strong>Tipp:</strong> Du brauchst keine speziellen Football-Cleats. Günstige Fußball-Stollen
              vom Decathlon oder Intersport tun es für den Anfang (20–50 EUR).
            </p>
          </div>

          {/* Handschuhe — OPTIONAL */}
          <div className="border-l-4 border-zinc-300 bg-bg-warm rounded-r-xl p-6 md:p-8">
            <span className="text-zinc-400 text-[10px] font-bold uppercase tracking-widest">Optional</span>
            <h2 className="text-navy text-xl md:text-2xl font-black mt-1 mb-2">Receiver-Handschuhe</h2>
            <p className="text-muted leading-relaxed">
              Receiver-Handschuhe verbessern den Grip beim Fangen erheblich — besonders bei nassem Wetter.
              Kein Muss, aber viele Spieler schwören drauf. Gibt es ab ca. 20 EUR.
            </p>
          </div>

          {/* Hütchen / Pylonen — OPTIONAL */}
          <div className="border-l-4 border-zinc-300 bg-bg-warm rounded-r-xl p-6 md:p-8">
            <span className="text-zinc-400 text-[10px] font-bold uppercase tracking-widest">Optional</span>
            <h2 className="text-navy text-xl md:text-2xl font-black mt-1 mb-2">Hütchen & Pylonen</h2>
            <p className="text-muted leading-relaxed">
              Zum Abstecken des Spielfelds und für Trainingsübungen. Für organisierte Spiele werden
              die meist vom Verein gestellt. Für Casual Games im Park: Ein Set mit 12 Hütchen kostet ca. 10 EUR.
            </p>
          </div>
        </div>

        {/* Summary */}
        <div className="bg-navy rounded-xl p-8 md:p-10 mb-16">
          <h2 className="text-white text-2xl font-black mb-4">Was kostet der Einstieg?</h2>
          <div className="grid sm:grid-cols-2 gap-6">
            <div>
              <h3 className="text-gold font-bold mb-2">Minimum (Einzelspieler)</h3>
              <ul className="text-white/60 text-sm space-y-1">
                <li>Sportschuhe: vorhanden / ~30 EUR</li>
                <li>Flag Belt: wird vom Team gestellt</li>
                <li className="text-white font-semibold pt-1">→ 0–30 EUR</li>
              </ul>
            </div>
            <div>
              <h3 className="text-gold font-bold mb-2">Komplettes Team-Set (10 Spieler)</h3>
              <ul className="text-white/60 text-sm space-y-1">
                <li>Flag Belt Set (10x): ~40 EUR</li>
                <li>2x Football: ~40 EUR</li>
                <li>Hütchen-Set: ~10 EUR</li>
                <li className="text-white font-semibold pt-1">→ ca. 90 EUR</li>
              </ul>
            </div>
          </div>
        </div>

        {/* CTA */}
        <section className="bg-bg-warm rounded-xl p-8 md:p-12 text-center">
          <h2 className="text-navy text-2xl md:text-3xl font-black mb-3">Equipment steht? Team finden!</h2>
          <p className="text-muted mb-6 max-w-md mx-auto">
            Über 500 Vereine in Deutschland suchen Spieler. Finde ein Team in deiner Nähe.
          </p>
          <Link
            href="/team-finden"
            className="bg-gold text-navy font-bold px-6 py-3 rounded-lg hover:bg-gold-hover transition-colors inline-block"
          >
            Team finden
          </Link>
        </section>
      </article>
    </main>
  );
}
```

- [ ] **Step 2: Verify**

```bash
npm run dev
```

Navigate to http://localhost:3000/ausruestung. Expected: Hero, intro, color-coded equipment cards (gold/navy/gray borders), pricing summary, CTA. Stop server.

- [ ] **Step 3: Commit**

```bash
git add src/app/ausruestung/page.tsx
git commit -m "feat: add Ausrüstung page with equipment guide and affiliate structure"
```

---

## Task 11: Page — Team finden (Interactive)

**Files:**
- Create: `src/components/TeamFinder.tsx`
- Create: `src/app/team-finden/page.tsx`

- [ ] **Step 1: Create TeamFinder client component**

Create `src/components/TeamFinder.tsx`:

```tsx
"use client";

import { useState, useMemo } from "react";

type Team = {
  name: string;
  city: string;
  state: string;
  lat: number;
  lng: number;
  league: string;
  website: string;
  gender: string[];
};

const STATES: Record<string, string> = {
  BW: "Baden-Württemberg",
  BY: "Bayern",
  BE: "Berlin",
  BB: "Brandenburg",
  HB: "Bremen",
  HH: "Hamburg",
  HE: "Hessen",
  MV: "Mecklenburg-Vorpommern",
  NI: "Niedersachsen",
  NRW: "Nordrhein-Westfalen",
  RP: "Rheinland-Pfalz",
  SL: "Saarland",
  SN: "Sachsen",
  ST: "Sachsen-Anhalt",
  SH: "Schleswig-Holstein",
  TH: "Thüringen",
};

export function TeamFinder({ teams }: { teams: Team[] }) {
  const [search, setSearch] = useState("");
  const [stateFilter, setStateFilter] = useState("");
  const [genderFilter, setGenderFilter] = useState("");

  const filtered = useMemo(() => {
    return teams.filter((team) => {
      const matchesSearch =
        !search ||
        team.name.toLowerCase().includes(search.toLowerCase()) ||
        team.city.toLowerCase().includes(search.toLowerCase());
      const matchesState = !stateFilter || team.state === stateFilter;
      const matchesGender = !genderFilter || team.gender.includes(genderFilter);
      return matchesSearch && matchesState && matchesGender;
    });
  }, [teams, search, stateFilter, genderFilter]);

  const availableStates = useMemo(() => {
    const states = new Set(teams.map((t) => t.state));
    return Array.from(states).sort();
  }, [teams]);

  return (
    <div>
      {/* Filters */}
      <div className="flex flex-col sm:flex-row gap-3 mb-8">
        <input
          type="text"
          placeholder="Stadt oder Teamname..."
          value={search}
          onChange={(e) => setSearch(e.target.value)}
          className="flex-1 px-4 py-2.5 border border-zinc-200 rounded-lg text-sm font-sans focus:outline-none focus:border-gold transition-colors"
        />
        <select
          value={stateFilter}
          onChange={(e) => setStateFilter(e.target.value)}
          className="px-4 py-2.5 border border-zinc-200 rounded-lg text-sm font-sans focus:outline-none focus:border-gold transition-colors bg-white"
        >
          <option value="">Alle Bundesländer</option>
          {availableStates.map((state) => (
            <option key={state} value={state}>
              {STATES[state] || state}
            </option>
          ))}
        </select>
        <select
          value={genderFilter}
          onChange={(e) => setGenderFilter(e.target.value)}
          className="px-4 py-2.5 border border-zinc-200 rounded-lg text-sm font-sans focus:outline-none focus:border-gold transition-colors bg-white"
        >
          <option value="">Alle Teams</option>
          <option value="men">Herren</option>
          <option value="women">Damen</option>
        </select>
      </div>

      {/* Results count */}
      <p className="text-muted text-sm mb-4">
        {filtered.length} {filtered.length === 1 ? "Team" : "Teams"} gefunden
      </p>

      {/* Team list */}
      {filtered.length === 0 ? (
        <div className="bg-bg-warm rounded-xl p-8 text-center">
          <p className="text-muted">Keine Teams gefunden. Versuche eine andere Suche.</p>
        </div>
      ) : (
        <div className="grid sm:grid-cols-2 gap-3">
          {filtered.map((team) => (
            <div
              key={`${team.name}-${team.city}`}
              className="bg-white border border-zinc-200 rounded-lg p-5 hover:border-gold transition-colors"
            >
              <div className="flex items-start justify-between gap-2">
                <div>
                  <h3 className="text-navy font-bold text-base">{team.name}</h3>
                  <p className="text-muted text-sm mt-0.5">
                    {team.city}, {STATES[team.state] || team.state}
                  </p>
                </div>
                <span className="text-[10px] font-bold uppercase tracking-wider text-muted bg-bg-warm px-2 py-1 rounded shrink-0">
                  {team.league}
                </span>
              </div>
              <div className="flex items-center gap-3 mt-3">
                <div className="flex gap-1.5">
                  {team.gender.map((g) => (
                    <span
                      key={g}
                      className="text-[10px] font-bold uppercase tracking-wider px-2 py-0.5 rounded"
                      style={{
                        background: g === "men" ? "rgba(0,43,92,0.08)" : "rgba(255,199,44,0.15)",
                        color: g === "men" ? "#002B5C" : "#b8860b",
                      }}
                    >
                      {g === "men" ? "Herren" : "Damen"}
                    </span>
                  ))}
                </div>
                {team.website && (
                  <a
                    href={team.website}
                    target="_blank"
                    rel="noopener noreferrer"
                    className="text-xs text-gold-hover font-semibold hover:underline ml-auto"
                  >
                    Website →
                  </a>
                )}
              </div>
            </div>
          ))}
        </div>
      )}

      {/* Note */}
      <p className="text-muted/50 text-xs mt-8 text-center">
        Diese Liste wird laufend erweitert. Dein Team fehlt?{" "}
        <a href="mailto:info@flagzone.de" className="underline hover:text-gold-hover">
          Schreib uns!
        </a>
      </p>
    </div>
  );
}
```

- [ ] **Step 2: Create Team Finden page**

Create `src/app/team-finden/page.tsx`:

```tsx
import type { Metadata } from "next";
import { TeamFinder } from "@/components/TeamFinder";
import teamsData from "@/data/teams.json";

export const metadata: Metadata = {
  title: "Flag Football Team finden — Vereine in deiner Nähe",
  description:
    "Finde Flag Football Teams und Vereine in deiner Nähe. Über 500 Vereine in ganz Deutschland — filtere nach Bundesland, Stadt und Team-Typ.",
  alternates: { canonical: "https://flagzone.de/team-finden" },
};

export default function TeamFinden() {
  return (
    <main>
      {/* Hero */}
      <section className="bg-navy">
        <div className="max-w-6xl mx-auto px-4 sm:px-6 py-14 md:py-20">
          <p className="text-gold text-xs font-bold uppercase tracking-widest mb-3">Community</p>
          <h1 className="text-white text-4xl sm:text-5xl md:text-6xl font-black leading-[0.95] tracking-tight max-w-3xl">
            Finde ein Team
            <span className="block text-gold">in deiner Nähe</span>
          </h1>
          <p className="text-white/50 text-lg mt-4 max-w-xl">
            Über 500 Vereine in ganz Deutschland. Suche nach Stadt, Bundesland oder Team.
          </p>
        </div>
      </section>

      <section className="max-w-6xl mx-auto px-4 sm:px-6 py-12 md:py-16">
        <TeamFinder teams={teamsData} />
      </section>
    </main>
  );
}
```

- [ ] **Step 3: Verify**

```bash
npm run dev
```

Navigate to http://localhost:3000/team-finden. Expected: Hero, search input, dropdown filters, team cards with badges. Search should filter in real time. Stop server.

- [ ] **Step 4: Commit**

```bash
git add src/components/TeamFinder.tsx src/app/team-finden/page.tsx
git commit -m "feat: add Team finden page with interactive search and filters"
```

---

## Task 12: Legal Pages — Impressum & Datenschutz

**Files:**
- Create: `src/app/impressum/page.tsx`
- Create: `src/app/datenschutz/page.tsx`

- [ ] **Step 1: Create Impressum page**

Create `src/app/impressum/page.tsx`:

```tsx
import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Impressum",
  description: "Impressum und Anbieterkennzeichnung von FlagZone.",
  robots: { index: false, follow: true },
  alternates: { canonical: "https://flagzone.de/impressum" },
};

export default function Impressum() {
  return (
    <main className="max-w-3xl mx-auto px-4 sm:px-6 py-12 md:py-16">
      <h1 className="text-navy text-3xl font-black tracking-tight mb-8">Impressum</h1>

      <div className="prose prose-zinc max-w-none space-y-6 text-muted leading-relaxed">
        <section>
          <h2 className="text-navy text-xl font-bold mb-2">Angaben gemäß § 5 TMG</h2>
          <p>
            [Vorname Nachname]<br />
            [Straße Hausnummer]<br />
            [PLZ Ort]<br />
            Deutschland
          </p>
        </section>

        <section>
          <h2 className="text-navy text-xl font-bold mb-2">Kontakt</h2>
          <p>
            E-Mail: info@flagzone.de
          </p>
        </section>

        <section>
          <h2 className="text-navy text-xl font-bold mb-2">Verantwortlich für den Inhalt nach § 55 Abs. 2 RStV</h2>
          <p>
            [Vorname Nachname]<br />
            [Adresse wie oben]
          </p>
        </section>

        <section>
          <h2 className="text-navy text-xl font-bold mb-2">Haftungsausschluss</h2>
          <h3 className="text-navy font-semibold text-base mb-1">Haftung für Inhalte</h3>
          <p>
            Die Inhalte unserer Seiten wurden mit größter Sorgfalt erstellt. Für die Richtigkeit,
            Vollständigkeit und Aktualität der Inhalte können wir jedoch keine Gewähr übernehmen.
          </p>
          <h3 className="text-navy font-semibold text-base mb-1 mt-4">Haftung für Links</h3>
          <p>
            Unser Angebot enthält Links zu externen Webseiten Dritter, auf deren Inhalte wir keinen
            Einfluss haben. Für die Inhalte der verlinkten Seiten ist stets der jeweilige Anbieter
            verantwortlich.
          </p>
        </section>

        <section>
          <h2 className="text-navy text-xl font-bold mb-2">Affiliate-Links</h2>
          <p>
            Diese Website enthält Affiliate-Links. Wenn du über diese Links Produkte kaufst, erhalten
            wir eine kleine Provision. Für dich ändert sich am Preis nichts. Affiliate-Links sind
            entsprechend gekennzeichnet.
          </p>
        </section>
      </div>
    </main>
  );
}
```

- [ ] **Step 2: Create Datenschutz page**

Create `src/app/datenschutz/page.tsx`:

```tsx
import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Datenschutzerklärung",
  description: "Datenschutzerklärung von FlagZone — Informationen zum Umgang mit deinen Daten.",
  robots: { index: false, follow: true },
  alternates: { canonical: "https://flagzone.de/datenschutz" },
};

export default function Datenschutz() {
  return (
    <main className="max-w-3xl mx-auto px-4 sm:px-6 py-12 md:py-16">
      <h1 className="text-navy text-3xl font-black tracking-tight mb-8">Datenschutzerklärung</h1>

      <div className="prose prose-zinc max-w-none space-y-6 text-muted leading-relaxed">
        <section>
          <h2 className="text-navy text-xl font-bold mb-2">1. Verantwortlicher</h2>
          <p>
            Verantwortlich für die Datenverarbeitung auf dieser Website ist:<br />
            [Vorname Nachname]<br />
            [Adresse]<br />
            E-Mail: info@flagzone.de
          </p>
        </section>

        <section>
          <h2 className="text-navy text-xl font-bold mb-2">2. Hosting</h2>
          <p>
            Diese Website wird bei Vercel Inc., 440 N Barranca Ave #4133, Covina, CA 91723, USA gehostet.
            Vercel verarbeitet dabei technisch notwendige Daten wie IP-Adressen und Zugriffsdaten.
            Weitere Informationen findest du in der{" "}
            <a href="https://vercel.com/legal/privacy-policy" target="_blank" rel="noopener noreferrer" className="text-navy underline decoration-gold decoration-2 underline-offset-2">
              Datenschutzerklärung von Vercel
            </a>.
          </p>
        </section>

        <section>
          <h2 className="text-navy text-xl font-bold mb-2">3. Analyse</h2>
          <p>
            Wir verwenden ein cookie-freies Analyse-Tool, das keine personenbezogenen Daten speichert
            und keine Cookies setzt. Es werden lediglich anonymisierte Zugriffsdaten erhoben
            (z.B. Seitenaufrufe, ungefährer Standort auf Länderebene). Ein Cookie-Banner ist daher nicht erforderlich.
          </p>
        </section>

        <section>
          <h2 className="text-navy text-xl font-bold mb-2">4. Affiliate-Links</h2>
          <p>
            Diese Website enthält Affiliate-Links zu Drittanbietern (z.B. Amazon.de). Wenn du auf
            einen Affiliate-Link klickst, setzt der jeweilige Anbieter möglicherweise Cookies auf
            deinem Gerät. Die Datenschutzbestimmungen des jeweiligen Anbieters gelten.
          </p>
        </section>

        <section>
          <h2 className="text-navy text-xl font-bold mb-2">5. Deine Rechte</h2>
          <p>
            Du hast das Recht auf Auskunft, Berichtigung, Löschung und Einschränkung der Verarbeitung
            deiner Daten sowie das Recht auf Datenübertragbarkeit. Bei Fragen wende dich an
            info@flagzone.de.
          </p>
        </section>

        <section>
          <h2 className="text-navy text-xl font-bold mb-2">6. Externe Schriftarten</h2>
          <p>
            Diese Website verwendet Google Fonts (Inter). Beim Aufruf der Seite wird eine Verbindung
            zu den Servern von Google LLC hergestellt, wobei deine IP-Adresse an Google übertragen wird.
            Weitere Informationen findest du in der{" "}
            <a href="https://policies.google.com/privacy" target="_blank" rel="noopener noreferrer" className="text-navy underline decoration-gold decoration-2 underline-offset-2">
              Datenschutzerklärung von Google
            </a>.
          </p>
        </section>
      </div>
    </main>
  );
}
```

- [ ] **Step 3: Verify both pages**

```bash
npm run dev
```

Navigate to http://localhost:3000/impressum and http://localhost:3000/datenschutz. Expected: Clean, readable legal text pages. Stop server.

- [ ] **Step 4: Commit**

```bash
git add src/app/impressum/page.tsx src/app/datenschutz/page.tsx
git commit -m "feat: add Impressum and Datenschutz legal pages"
```

---

## Task 13: Vercel Analytics & Final Config

**Files:**
- Modify: `src/app/layout.tsx`
- Modify: `next.config.mjs`

- [ ] **Step 1: Install Vercel Analytics**

```bash
npm install @vercel/analytics
```

- [ ] **Step 2: Add Analytics to layout**

In `src/app/layout.tsx`, add the import at the top:

```tsx
import { Analytics } from "@vercel/analytics/react";
```

Then inside the `<body>` tag, after `<Footer />`, add:

```tsx
<Analytics />
```

The full body becomes:
```tsx
<body className="min-h-screen flex flex-col">
  <Nav />
  {children}
  <Footer />
  <Analytics />
</body>
```

- [ ] **Step 3: Update next.config.mjs for JSON imports**

Replace `next.config.mjs`:

```javascript
/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "export",
};

export default nextConfig;
```

Note: `output: "export"` generates a fully static site. This is appropriate since we have no dynamic server routes. If you later need API routes or ISR, remove this option.

Wait — the sitemap.ts and robots.ts use dynamic route handlers which are NOT compatible with `output: "export"`. Remove the output option and let Vercel handle the deployment mode:

```javascript
/** @type {import('next').NextConfig} */
const nextConfig = {};

export default nextConfig;
```

- [ ] **Step 4: Build and verify**

```bash
npm run build
```

Expected: Build completes with no errors. All pages listed as static routes.

- [ ] **Step 5: Commit**

```bash
git add src/app/layout.tsx next.config.mjs package.json package-lock.json
git commit -m "feat: add Vercel Analytics and finalize config"
```

---

## Task 14: Build Verification & Deploy Prep

- [ ] **Step 1: Full build**

```bash
npm run build
```

Expected: All pages render as static HTML. No TypeScript errors, no build warnings.

- [ ] **Step 2: Run dev server and verify all pages**

```bash
npm run dev
```

Visit each route and verify:
- http://localhost:3000/ — Homepage with hero, stats, cards, events, teasers
- http://localhost:3000/was-ist-flag-football — Rules with field diagram, positions, FAQ
- http://localhost:3000/olympia-2028 — Timeline, format, Germany's chances
- http://localhost:3000/wm-2026-duesseldorf — Event details, schedule, venue
- http://localhost:3000/ausruestung — Equipment guide with priority indicators
- http://localhost:3000/team-finden — Search/filter working, team cards display
- http://localhost:3000/impressum — Legal text
- http://localhost:3000/datenschutz — Privacy text

Check mobile responsiveness on each page. Stop server.

- [ ] **Step 3: Push to GitHub**

```bash
git push origin main
```

- [ ] **Step 4: Deploy to Vercel**

```bash
npx vercel --prod
```

Or link the GitHub repo via the Vercel dashboard for automatic deployments on push.

- [ ] **Step 5: Commit any final fixes**

If anything needed adjustment during verification, commit the fixes:

```bash
git add -A
git commit -m "fix: polish from build verification"
git push origin main
```
